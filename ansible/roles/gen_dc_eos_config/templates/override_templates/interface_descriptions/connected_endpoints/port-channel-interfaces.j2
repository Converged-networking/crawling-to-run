{% if adapter_settings.type == 'core' %}
{{ adapter_settings.type }}: {{ peer }} {{ adapter_port_channel_description }}
{% elif adapter_settings.type == 'cust' %}
{{ adapter_settings.type }}: {{ connected_endpoints[endpoint_key][connected_endpoint].service }} ({{ peer }} {{ adapter_port_channel_description }})
{% else %}
{# Force error on unknown adapter type #}
{{ 0/0 }}
{% endif %}
